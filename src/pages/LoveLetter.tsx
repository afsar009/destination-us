import { FiChevronRight } from 'react-icons/fi'; import GlassCard from '../components/GlassCard'; import PrimaryButton from '../components/PrimaryButton'; import { useJourney } from '../hooks/useJourney';
export default function LoveLetter(){const go=useJourney(); return <div className="mx-auto flex min-h-screen max-w-2xl items-center px-6 py-24"><GlassCard><p className="eyebrow">For you</p><h1 className="mt-6 font-display text-5xl leading-none sm:text-6xl">My favorite place<br/>has always been<br/><i className="text-blush">next to you.</i></h1><div className="mt-8 space-y-4 text-sm leading-7 text-mist/65"><p>I love the way ordinary days become stories when you are in them.</p><p>So here is a promise: more detours, more late dinners, more moments that don’t fit in photographs.</p></div><p className="mt-8 font-display text-2xl italic text-champagne">With all my heart, always.</p><PrimaryButton className="mt-9" onClick={()=>go('/gallery')}>Open our memories <FiChevronRight className="ml-1 inline"/></PrimaryButton></GlassCard></div>}
