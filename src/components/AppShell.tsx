import { ReactNode, useEffect } from 'react'; import { useLocation } from 'react-router-dom'; import { AnimatePresence, motion } from 'framer-motion'; import Progress from './Progress';
export default function AppShell({ children }: { children: ReactNode }) { const { pathname } = useLocation(); useEffect(() => window.scrollTo(0, 0), [pathname]); return <main className="relative isolate min-h-screen overflow-hidden bg-ink"><div className="orb -left-32 top-16 h-80 w-80 bg-blush/15"/><div className="orb -right-20 bottom-0 h-96 w-96 bg-champagne/10"/><Progress/><AnimatePresence mode="wait"><motion.div key={pathname} initial={{opacity:0,y:16}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-10}} transition={{duration:.5,ease:[.22,1,.36,1]}}>{children}</motion.div></AnimatePresence></main> }
