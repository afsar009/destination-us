import { useState } from 'react'; import { motion } from 'framer-motion'; import GlassCard from '../components/GlassCard'; import PrimaryButton from '../components/PrimaryButton'; import { useJourney } from '../hooks/useJourney';
export default function Question(){const go=useJourney(),[answer,setAnswer]=useState<string>(); const options=['Somewhere with ocean air','A city that stays awake','Anywhere, if it’s with you']; return <div className="mx-auto flex min-h-screen max-w-xl items-center px-6 py-24"><div className="w-full"><p className="eyebrow">A small question</p><h1 className="display mt-5">Where do you feel<br/>most <i className="text-champagne">alive?</i></h1><div className="mt-10 space-y-3">{options.map((option,i)=><motion.button key={option} whileTap={{scale:.98}} onClick={()=>setAnswer(option)} className={`glass w-full rounded-2xl p-5 text-left text-sm transition ${answer===option?'border-champagne bg-champagne/10 text-champagne':'text-mist/70'}`}><span className="mr-4 font-display text-xl text-champagne/60">0{i+1}</span>{option}</motion.button>)}</div>{answer&&<GlassCard className="mt-6"><p className="text-sm text-mist/70">Perfect. I had a feeling you’d say that.</p><PrimaryButton className="mt-5" onClick={()=>go('/booking')}>Reveal our plans</PrimaryButton></GlassCard>}</div></div>}
