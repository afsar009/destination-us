# Destination Us

A premium, installable React PWA for an intimate travel-inspired invitation.

## Run locally

```bash
npm install
npm run dev
```

Use `npm run build` for a production build. The application is configured as a PWA with automatic service-worker updates.
