import { FiArrowRight } from 'react-icons/fi'; import GlassCard from '../components/GlassCard'; import PrimaryButton from '../components/PrimaryButton'; import { useJourney } from '../hooks/useJourney';
export default function Intro(){const go=useJourney(); return <div className="mx-auto flex min-h-screen max-w-4xl items-center px-6 py-24"><div className="grid w-full gap-8 md:grid-cols-[1.2fr_.8fr] md:items-end"><div><p className="eyebrow">Chapter one</p><h1 className="display mt-5">A little<br/>closer to <i className="text-blush">us.</i></h1></div><GlassCard><p className="font-display text-3xl leading-tight text-mist">“The best part of a destination is who you arrive with.”</p><p className="mt-5 text-sm text-mist/50">This is an invitation to slow down, look up, and choose each other—again.</p><PrimaryButton className="mt-8" onClick={()=>go('/question')}>Continue <FiArrowRight className="ml-2 inline"/></PrimaryButton></GlassCard></div></div>}
