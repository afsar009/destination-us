import { FiClock } from 'react-icons/fi'; import GlassCard from '../components/GlassCard'; import PrimaryButton from '../components/PrimaryButton'; import { itinerary } from '../utils/journey'; import { useJourney } from '../hooks/useJourney';
export default function Restaurant(){const go=useJourney(); return <div className="mx-auto flex min-h-screen max-w-3xl items-center px-6 py-24"><div className="w-full"><p className="eyebrow">The first stop</p><h1 className="display mt-5">A table set<br/>for <i className="text-champagne">possibility.</i></h1><div className="mt-10 space-y-3">{itinerary.map(({time,label,detail})=><GlassCard key={time} className="flex items-start gap-5 py-5"><FiClock className="mt-1 text-champagne"/><div><p className="text-xs tracking-widest text-champagne/70">{time}</p><h2 className="mt-1 font-display text-2xl">{label}</h2><p className="mt-1 text-sm text-mist/50">{detail}</p></div></GlassCard>)}</div><PrimaryButton className="mt-8" onClick={()=>go('/boarding-pass')}>Your boarding pass</PrimaryButton></div></div>}
