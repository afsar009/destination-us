import { useEffect, useRef } from 'react'; import { FiArrowUpRight } from 'react-icons/fi'; import PrimaryButton from '../components/PrimaryButton'; import { useJourney } from '../hooks/useJourney'; import { pulseHeart } from '../animations/heart';
export default function Splash(){const go=useJourney(), heart=useRef<HTMLSpanElement>(null); useEffect(()=>{const tween=heart.current&&pulseHeart(heart.current); return()=>tween?.kill()},[]); return <div className="flex min-h-screen flex-col justify-end px-6 pb-10 pt-28 sm:px-12 sm:pb-14"><div className="mb-auto"><p className="eyebrow">A private invitation · 2026</p></div><span ref={heart} className="mb-8 block text-4xl text-blush">♥</span><h1 className="display max-w-3xl text-mist">There’s a place<br/><i className="text-champagne">we haven’t been yet.</i></h1><p className="mt-7 max-w-md text-sm leading-7 text-mist/60">Not on any map. Not in any guidebook. Just waiting for the two of us.</p><PrimaryButton className="mt-10 w-fit" onClick={()=>go('/intro')}>Begin the journey <FiArrowUpRight className="ml-2 inline"/></PrimaryButton></div>}
