import { motion } from 'framer-motion'; import PrimaryButton from '../components/PrimaryButton'; import { gallery } from '../utils/journey'; import { useJourney } from '../hooks/useJourney';
export default function Gallery(){const go=useJourney(); return <div className="mx-auto min-h-screen max-w-4xl px-6 py-24"><p className="eyebrow">Tiny archives</p><h1 className="display mt-5">Proof that<br/>we were <i className="text-champagne">here.</i></h1><div className="mt-10 grid grid-cols-2 gap-3 sm:grid-cols-4">{gallery.map((caption,i)=><motion.figure key={caption} whileHover={{y:-6}} className={`glass flex aspect-[3/4] flex-col justify-end rounded-2xl p-4 ${i%2?'mt-8':''}`}><div className={`mb-auto h-16 w-16 rounded-full blur-xl ${i%2?'bg-blush/40':'bg-champagne/30'}`}/><figcaption className="font-display text-xl leading-tight">{caption}</figcaption><span className="mt-2 text-[10px] uppercase tracking-widest text-mist/40">Memory 0{i+1}</span></motion.figure>)}</div><PrimaryButton className="mt-10" onClick={()=>go('/finale')}>One last thing</PrimaryButton></div>}
